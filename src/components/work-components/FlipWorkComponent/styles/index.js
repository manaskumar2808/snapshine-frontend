import styled from 'styled-components/macro';

export const Container = styled.div``;

export const FlipInputSection = styled.div`
    color: ${({ theme }) => `${theme.colors.dark}`};
    /* border: ${({ theme }) => `0.5px solid ${theme.colors.gray}`}; */
    border-radius: 5px;
    padding: 5px;
`;