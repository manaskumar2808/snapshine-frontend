import styled from 'styled-components/macro';

export const Container = styled.div`
    margin-top: 20px;
`;

export const RotateInputSection = styled.div`
    color: ${({ theme }) => `${theme.colors.dark}`};
    border-radius: 5px;
    /* padding: 5px; */
`;